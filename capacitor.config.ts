import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mojo',
  appName: 'mojo-velo-angular',
  webDir: 'dist/mojo-velo-angular/browser'
};

export default config;
